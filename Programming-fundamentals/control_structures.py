# Control Structures/ Control Flow
# instance_running = "broken"
# If Statements
# if instance_running == "running":
#    print("Instance is running")
#elif instance_running == "stopped":
#    print("Instance is stopped")
# else:
#    print("Instance is in an unknown state")

public_access_block = False
if public_access_block:
    print("Public access is blocked")
elif public_access_block == False:
    print("Public access is not blocked")
else:
    print("Public access is in an unknown state")